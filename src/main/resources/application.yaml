spring:
  application:
    name: todolist

  datasource:
    url: jdbc:mysql://localhost:3306/todolist?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
    username: ${DATABASE_USER:root}
    password: ${DATABASE_PASSWORD:tamara123}
    # driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop